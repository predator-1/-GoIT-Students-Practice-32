{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3030",
    "start:auth": "json-server --watch db.json --port 3030 -m ./node_modules/json-server-auth"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3",
    "json-server-auth": "^2.0.2"
  }
}
